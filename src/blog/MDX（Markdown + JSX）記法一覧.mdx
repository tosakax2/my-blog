---
title: MDX（Markdown + JSX）記法一覧
date: 2025-07-07
categories:
  - Tech
tags:
  - MDX
  - CheatSheet
  - SyntaxGuide
---

簡易的な MDX（.mdx）の記法一覧です。Markdown（.md）とほぼ同じ構文が使えます。

## 見出し

```markdown
## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

###### Heading 6
```

## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

###### Heading 6

## 段落と改行

```markdown
これは一つ目の段落です。

これは二つ目の段落です。
```

これは一つ目の段落です。

これは二つ目の段落です。

改行には二つの方法があります：

1. 行末に半角スペースを二つ以上入れる
1. 行末に `<br />` タグを入れる

```markdown
ここの文の後に改行が入ります。  
ここは次の行です。

この分の後にも改行が入ります。<br />
ここも次の文です。
```

ここの文の後に改行が入ります。  
ここは次の行です。

この分の後にも改行が入ります。<br />
ここも次の文です。

**注意**：半角スペースによる改行は目視できないため、トラブルの原因になる可能性があります。

## 強調（太字・斜体）

```markdown
_斜体_

**太字**

**_太字かつ斜体_**
```

_斜体_

**太字**

**_太字かつ斜体_**

## リスト

### 順序なしリスト（箇条書き）

```markdown
- 親項目 1
- 親項目 2
  - 子項目１
  - 子項目２
    - 孫項目１
- 親項目 3
```

- 親項目 1
- 親項目 2
  - 子項目１
  - 子項目２
    - 孫項目１
- 親項目 3

### 順序付きリスト（番号付き）

```markdown
1. 親項目 1
1. 親項目 2
   1. 子項目１
   1. 子項目２
      1. 孫項目１
1. 親項目 3
```

1. 親項目 1
1. 親項目 2
   1. 子項目１
   1. 子項目２
      1. 孫項目１
1. 親項目 3

## リンク

### 基本的なリンク

```markdown
[リンクテキスト](https://www.example.com)
```

[リンクテキスト](https://www.example.com)

### タイトル付きリンク

```markdown
[リンクテキスト](https://www.example.com "リンクのツールチップ")
```

[リンクテキスト](https://www.example.com "リンクのツールチップ")

### 参照リンク

```markdown
[リンクテキスト][参照ID]

[参照ID]: https://www.example.com "リンクのツールチップ"
```

[リンクテキスト][参照ID]

[参照ID]: https://www.example.com "リンクのツールチップ"

### 相対リンク

```markdown
[同じリポジトリの別ファイル](../path/to/file.md)
```

### メールリンク

```markdown
email@example.com
```

email@example.com

## 画像

### 基本的な画像挿入

```markdown
![代替テキスト](https://example.com/image.jpg)
```

![代替テキスト](https://example.com/image.jpg)

### ツールチップ 付き画像

```markdown
![代替テキスト](https://example.com/image.jpg "画像のツールチップ")
```

![代替テキスト](https://example.com/image.jpg "画像のツールチップ")

### 参照形式の画像

```markdown
![代替テキスト][画像ID]

[画像ID]: https://example.com/image.jpg "画像のツールチップ"
```

![代替テキスト][画像ID]

[画像ID]: https://example.com/image.jpg "画像のツールチップ"

### リンク付き画像

```markdown
[![代替テキスト](https://example.com/image.jpg)](https://www.example.com)
```

[![代替テキスト](https://example.com/image.jpg)](https://www.example.com)

## 引用

```markdown
> これは引用です。
> 複数行に渡ることもあります。
>
> > ネストされた引用も可能です。
```

> これは引用です。
> 複数行に渡ることもあります。
>
> > ネストされた引用も可能です。

## コード

### インラインコード

```markdown
`インラインコード` を使用できます。
```

`インラインコード` を使用できます。

### コードブロック

#### フェンスコードブロック

````markdown
```
// 言語指定なしのコードブロック
function example() {
return true;
}
```
````

````plaintext
```javascript
// 言語指定によるシンタックスハイライト
function example() {
  return true;
}
```
````

表示例：

```javascript
// 言語指定によるシンタックスハイライト
function example() {
  return true;
}
```

## 水平線

```markdown
---
```

---

## 表

```markdown
| 見出し 1 | 見出し 2 | 見出し 3 |
| -------- | -------- | -------- |
| セル 1   | セル 2   | セル 3   |
| セル 4   | セル 5   | セル 6   |
```

| 見出し 1 | 見出し 2 | 見出し 3 |
| -------- | -------- | -------- |
| セル 1   | セル 2   | セル 3   |
| セル 4   | セル 5   | セル 6   |

### 表の整列

```markdown
| 左揃え | 中央揃え | 右揃え |
| :----- | :------: | -----: |
| 左     |   中央   |     右 |
| 左揃え | 中央揃え | 右揃え |
```

| 左揃え | 中央揃え | 右揃え |
| :----- | :------: | -----: |
| 左     |   中央   |     右 |
| 左揃え | 中央揃え | 右揃え |

## タスクリスト

```markdown
- [x] 完了したタスク
- [ ] 未完了のタスク
- [ ] ~~取り消されたタスク~~
```

- [x] 完了したタスク
- [ ] 未完了のタスク
- [ ] ~~取り消されたタスク~~

## 脚注

これは脚注付きのテキストです[^1]。

[^1]: これは脚注の内容です。

## 取り消し線

```markdown
~~取り消し線~~
```

~~取り消し線~~

## 上付き・下付き文字

```markdown
H<sub>2</sub>O（水）

E=mc<sup>2</sup>（アインシュタイン方程式）
```

H<sub>2</sub>O（水）

E=mc<sup>2</sup>（アインシュタイン方程式）

## 自動リンク

```markdown
https://www.example.com
```

https://www.example.com

**注意**：Markdown では `<>` で囲むと自動リンクになりますが、MDX では `<>` で囲んだ中に `/` が含まれていると、タグとして解釈されてエラーになる可能性があります。

## HTML の埋め込み

```markdown
<div style="color: red;">
  これは<em>HTML</em>で書かれた<strong>赤文字</strong>です。
</div>
```

<div style="color: red;">
  これは<em>HTML</em>で書かれた<strong>赤文字</strong>です。
</div>

## エスケープ文字

```markdown
\*これはイタリック体ではなく、アスタリスクで囲まれたテキストです\*
```

\*これはイタリック体ではなく、アスタリスクで囲まれたテキストです\*

エスケープできる文字：

```plaintext
\ バックスラッシュ
` バッククォート
* アスタリスク
_ アンダースコア
{} 中括弧
[] 角括弧
() 丸括弧
# ハッシュ記号
+ プラス記号
- マイナス記号（ハイフン）
. ピリオド
! 感嘆符
```

## 数式（LaTeX）

```markdown
インライン数式：$E=mc^2$

ブロック数式：

$$
\frac{d}{dx}e^x = e^x
$$
```

インライン数式：$E=mc^2$

ブロック数式：

$$
\frac{d}{dx}e^x = e^x
$$
